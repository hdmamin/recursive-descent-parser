---
expected_error_type: compile
---
// Function parameters are considered variables in
// the function's scope
fun foo(a) {
  // Attempting to declare a variable with same
  // name as parameter
  var a; // expect compile error
}