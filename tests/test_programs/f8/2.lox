---
expected_error_type: none
---
// This program uses a while loop to count down
// from <<RANDOM_DIGIT_1>> to 1, printing each
// number
// and then decrementing the count until it
// reaches 0, at which point it prints
// "Blast off!"
var count = <<RANDOM_DIGIT_1>>;

fun tick() {
  if (count > 0) {
    print count;
    count = count - 1;
    return false;
  }
  print "Blast off!";
  return true;
}

while (!tick()) {}